'use client';

interface ProgressStatsProps {
  currentAmount: number;
  monthlyGoal: number;
  percentage: number;
}

export function ProgressStats({ currentAmount, monthlyGoal, percentage }: ProgressStatsProps) {
  return null; // Stats removed for minimal design
}

